{% extends 'base.html.twig' %}

{% block title %}{{ controller_name }}{% endblock %}

{% block content %}
<div class="example-wrapper">
    <p>Le nom de la catégorie : {{ category.name }} </p>

    <table class="table">
    <thead>
        <tr>
        <th scope="col">#</th>
        <th scope="col">Posts</th>
        </tr>
    </thead>
    <tbody>
    {% for post in posts %}
        <tr>
        <th scope="row">{{ post.id }}</th>
        <td><a href={{ path("app_post", { id: post.id }) }}>{{ post.title }}</a></td>
        </tr>
        {% endfor %}
    </tbody>
    </table>
</div>
{% endblock %}
